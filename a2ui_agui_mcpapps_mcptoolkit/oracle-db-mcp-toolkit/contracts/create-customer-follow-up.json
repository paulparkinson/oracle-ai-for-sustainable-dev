{
  "name": "create-customer-follow-up",
  "description": "Creates one approved follow-up action through the input-only MCP stored procedure after application-level approval.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["actionId", "customerId", "actionType", "actionNotes", "requestedBy"],
    "properties": {
      "actionId": { "type": "integer", "minimum": 1, "description": "Identifier reserved by the preceding bounded MCP sequence tool." },
      "customerId": { "type": "integer", "minimum": 1 },
      "actionType": { "type": "string", "enum": ["REVIEW", "CONTACT_OWNER", "FREEZE_CHANGES"] },
      "actionNotes": { "type": "string", "minLength": 3, "maxLength": 2000 },
      "requestedBy": { "type": "string", "minLength": 3, "maxLength": 200 }
    }
  }
}
