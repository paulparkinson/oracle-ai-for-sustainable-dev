For compatible conversational hosts, the optional MCP App registers a sandboxed dashboard resource. Sandboxing is part of the MCP Apps host model, not merely a demo choice. Structured account data crosses the host bridge, while database credentials do not.
