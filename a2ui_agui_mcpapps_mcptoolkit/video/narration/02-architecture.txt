The browser receives AG-UI events and A2UI interface updates. The Java service owns orchestration, validation, and human approval. The MCP Toolkit exposes governed database capabilities that multiple agents and MCP-compatible clients can reuse.
